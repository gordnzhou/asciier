

const ASCII_CHARACTERS = " .:-=+*#%@";

const MAX_PIXELS_PER_FRAME = 12000;
const WIDTH_SCALE_FACTOR = 1.8;

const FPS = 30;
const PIXELS_PER_CHAR = 4;

export const convertToASCII = (videoFile) => {
    
}